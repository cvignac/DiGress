# @package _global_
general:
    name : 'regressor'
    gpus : 0
    wandb: 'online'
    remove_h: True
    guidance_target: 'mu'                    # 'mu', 'homo', 'both', 'no-target'
    check_val_every_n_epochs: 1
train:
    n_epochs: 2
    batch_size: 512
    save_model: True
model:
    n_layers: 2
    lambda_train: [2, 0]
    extra_features: null
